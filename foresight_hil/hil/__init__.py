"""HIL-SERL-*style* training utilities (simulated oracle, NOT a real human).

Contains the RLPD-style mixed demo/online replay buffer and the intervention
controller that routes WHO acts (learner vs scripted oracle) through the
FORESIGHT-HIL VoI trigger under a query budget.
"""
